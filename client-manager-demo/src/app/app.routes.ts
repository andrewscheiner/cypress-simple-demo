import { Routes } from '@angular/router';
import { ClientManager } from './features/client-manager/client-manager';

export const routes: Routes = [
  {
    path: '',
    component: ClientManager
  }
];
